--- noble.orig/src/utils/http_request/http_request_test.go
+++ noble/src/utils/http_request/http_request_test.go
@@ -24,5 +24,5 @@ func TestFetchURLContent_Success(t *test
 func TestFetchURLContent_InvalidURL(t *testing.T) {
 	_, err := FetchURLContent("http://invalid-url.test.does.not-exists")
 	require.Error(t, err)
-	require.Contains(t, err.Error(), "device or resource busy")
+	require.Contains(t, err.Error(), "no such host")
 }
